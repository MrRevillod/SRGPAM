import React from "react"
import PageLayout from "../../layouts/PageLayout"

const CentersPage: React.FC = () => {
	return (
		<PageLayout pageTitle="Centros de atención" addFunction={() => {}} setData={() => {}}>
			<></>
		</PageLayout>
	)
}

export default CentersPage
